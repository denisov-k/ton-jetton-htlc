node_modules/
# fetched at build time from ton-blockchain (see `npm run deps`)
contracts/stdlib.fc
contracts/jetton/
# compiled code cells
*.boc.b64
# run state — holds swap secrets
state*.json
frc-main-state.json
# never: this is a live key
*wallet.json
driver/config.json
driver/swaps/
driver/config.*.json
